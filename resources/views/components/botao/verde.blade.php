<button type="{{ $type }}"
    class="flex flex-nowrap self-center rounded-md bg-green-600 hover:bg-green-900 text-white px-3 py-2 uppercase ">
    <i class="{{ $icon ?? '' }}"> </i> {{ $title ?? ""}}
</button>

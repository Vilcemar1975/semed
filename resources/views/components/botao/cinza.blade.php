<button type="{{ $type }}"
    class="flex flex-nowrap self-center rounded-md bg-gray-600 hover:bg-gray-900 text-white px-3 py-1 uppercase">
    <i class="{{ $icon ?? '' }}"> </i> {{ $title ?? ""}}
</button>
